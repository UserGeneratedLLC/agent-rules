---
globs: **/*.py
alwaysApply: false
---

# Python Code Structure

- Type function parameters and return values; skip where inference is obvious
- Use `pathlib` for file paths
- Prefer built-in libraries when suitable
- Dependencies: modern, maintained packages only
